language: java
jdk:
  - oraclejdk9
branches:
  only:
    - master
    - develop
dist: trusty
notifications:
  email:
    recipients:
      - florenciasalcedo13@gmail.com
script:
  #Test unitario y de integracion con cobertura (jacoco)
  - mvn org.jacoco:jacoco-maven-plugin:prepare-agent verify

  #SONARCLOUD
  - mvn sonar:sonar -Dsonar.host.url=https://sonarcloud.io -Dsonar.organization=florsalcedowd -Dsonar.login=$SONAR
